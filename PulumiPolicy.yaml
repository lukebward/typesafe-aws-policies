name: typesafe-aws-policies
version: 0.1.0
description: AWS good-usage policies where each rule is a plain-English question answered by TypeSafe's Jev model.
runtime:
  name: python
  options:
    virtualenv: venv
